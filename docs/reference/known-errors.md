# Known Errors

Bug history has moved to **[docs/bugs/README.md](../bugs/README.md)**. For every bug investigation, read that index first, then the matching issue detail. Record each fix in its detail and index row; do not add new entries here or create root-level fix summaries.

This file only preserves the old entry point. The recording workflow and template live in the new index.

### `BREAKDOWN_INVALID_JSON`

The original record is now [分镜拆解 JSON 响应兼容](../bugs/2026-09-01-breakdown-invalid-json.md), including its source, regression entry points, and verification limits.
