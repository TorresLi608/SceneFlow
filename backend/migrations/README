Alembic migrations for the SQLModel schema.

From `backend/`:

    .venv/bin/alembic revision --autogenerate -m "description"
    .venv/bin/alembic upgrade head
    .venv/bin/alembic check

Review autogenerated revisions before applying them. `init_db()` runs `upgrade head`
at application startup.
