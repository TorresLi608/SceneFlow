{
  "name": "sceneflow",
  "private": true,
  "engines": {
    "node": ">=22",
    "pnpm": ">=10"
  },
  "scripts": {
    "dev:frontend": "pnpm --dir frontend dev",
    "dev:backend": "cd backend && .venv/bin/python -m uvicorn app:app --host 0.0.0.0 --port 8080"
  }
}